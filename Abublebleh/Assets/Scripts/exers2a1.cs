using UnityEngine;

public class exers2a1 : MonoBehaviour
{

    [SerializeField] private int kills;


    // Start is called once before the first execution of Update after the MonoBehaviour is created 
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
